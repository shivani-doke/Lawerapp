name: LegalAI
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  file_picker: ^8.0.0+1
  http_parser: ^4.0.2
  path_provider: ^2.1.5
  pdf: ^3.10.7
  universal_html: ^2.2.4 
  universal_io: ^2.2.2 
  docx_template: ^0.4.0
  archive: ^3.4.0
  syncfusion_flutter_pdfviewer: ^24.1.41
  url_launcher: ^6.2.5
  flutter_markdown: ^0.7.0
  webview_windows: ^0.4.0
  speech_to_text: ^7.0.0
  shared_preferences: ^2.3.2
  

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/templates/poa_template.docx
